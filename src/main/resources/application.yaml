server:
  port: ${SERVER_PORT:8000}

eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_CLIENT_SERVICEURL_DEFAULTZONE:http://localhost:8761/eureka}

spring:
  application:
    name: iti-service-auth
  config.import: optional:configserver:${SPRING_CLOUD_CONFIG_URI:http://localhost:8888}
